import { FC } from 'react';

import styles from './App.module.scss';

const App: FC = () => {
  return (
    <div className={styles.container}>
      <h1>Chat app</h1>
    </div>
  );
};

export default App;
