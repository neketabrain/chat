export const DB_USERNAME = process.env.POSTGRES_USER || 'postgres';

export const DB_PASSWORD = process.env.POSTGRES_USER || 'postgres';

export const DB_NAME = process.env.POSTGRES_DB || 'chat-db';

export const DB_PORT = process.env.POSTGRES_PORT || '5432';
